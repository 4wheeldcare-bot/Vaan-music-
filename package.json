{
  "name": "eva-music-workspace",
  "private": true,
  "scripts": {
    "dev": "cd \"landing page/eva-music-app\" && npm install && npm run dev",
    "build": "cd \"landing page/eva-music-app\" && npm run build",
    "build:frontend": "cd \"landing page/eva-music-app\" && npm run build",
    "lint:frontend": "cd \"landing page/eva-music-app\" && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
